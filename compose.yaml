services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: open3dcalc-web:local
    container_name: open3dcalc-web
    ports:
      - "8090:8090"
    restart: unless-stopped
